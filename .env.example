DATABASE_URL=postgresql+asyncpg://spacecdf:spacecdf_dev@localhost:5432/spacecdf
REDIS_URL=redis://localhost:6379

# AI Advisor — Claude API integration
# Set this to enable AI-assisted design mode.
# The key is NEVER committed to git (in .gitignore).
# Users can also pass it via X-API-Key header from the frontend.
# ANTHROPIC_API_KEY=sk-ant-...

# AI token budget (per session, in tokens)
AI_TOKEN_BUDGET=100000
AI_MODEL=claude-sonnet-4-20250514
